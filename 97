--97
select last_name, job_id, EMPLOYEES.DEPARTMENT_ID, department_name from EMPLOYEES JOIN DEPARTMENTS ON DEPARTMENTS.DEPARTMENT_ID = EMPLOYEES.DEPARTMENT_ID
JOIN LOCATIONS on LOCATIONS.LOCATION_ID = DEPARTMENTS.LOCATION_ID 
where city = 'Toronto'